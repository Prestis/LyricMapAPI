services:
  api:
    build: .
    container_name: lyricmap-api
    ports:
      - "8000:8000"
    volumes:
      - ./lyricmap.db:/app/lyricmap.db
    env_file:
      - .env
    restart: unless-stopped
